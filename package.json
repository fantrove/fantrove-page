{
  "name": "fantrove-header-ts",
  "version": "1.0.0",
  "description": "Fantrove header modules converted to TypeScript (build setup)",
  "private": true,
  "scripts": {
    "clean": "rimraf build && rimraf assets/js/header-modules/** assets/js/header*.js || true",
    "build:ts": "tsc -p tsconfig.json",
    "build:copy": "node scripts/copy-built.js",
    "build": "npm run clean && npm run build:ts && npm run build:copy",
    "watch": "tsc -p tsconfig.json --watch",
    "prettier:check": "echo \"(optional) run prettier manually\""
  },
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "rimraf": "^5.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}